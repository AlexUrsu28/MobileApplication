<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LicenseActivity"
        tools:targetApi="31">

        <!-- Activitatea principală (de start) a aplicației -->
        <activity
            android:name=".SignUpActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LoginActivity urmează după SignUpActivity -->
        <activity
            android:name=".LoginActivity"
            android:parentActivityName=".SignUpActivity"
            android:exported="false" />

        <!-- MainActivity urmează după LoginActivity -->
        <activity
            android:name=".MainActivity"
            android:parentActivityName=".LoginActivity"
            android:exported="false" />

        <!-- AddActivity urmează după MainActivity -->
        <activity
            android:name=".AddActivity"
            android:parentActivityName=".MainActivity"
            android:exported="false" />

    </application>

</manifest>